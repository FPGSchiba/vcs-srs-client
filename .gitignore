build/bin
bin/
node_modules
frontend/dist
log/
**/wailsjs/go/**
.idea/
.task/

# --- VCS Client additions ---

# generated (not covered above)
srspb/
frontend/bindings/

# Task 12: the real vcs-srs-server, checked out by CI (and optionally by
# hand, locally) to build VCS_SERVER_BIN for internal/voice's integration
# suite. Never vendored.
.vcs-srs-server/

# runtime artefacts
*.log

# local-only configs that should never be committed
session.json
config.toml.local

# IDE
.vscode/
.superpowers/
